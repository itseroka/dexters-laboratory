<p style="color: green"><%= notice %></p>

<%= render @category %>

<br />

<div class="buttons">
<%= button_to "Back to categories", categories_path, method: :get, type: :button, class: "button" %>

<% if user_session %>
  <%= button_to "Edit this category", edit_category_path(@category), method: :get, type: :button, class: "button"%>

  <%= button_to "Destroy this category", @category, method: :delete, type: :button, class: "button"%>
  <% end %>
  </div>