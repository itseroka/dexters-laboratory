<div id="<%= dom_id category %>">
  <p>
    <strong>Name:</strong>
    <%= category.name %>
  </p>
  <% category.posts.each do |post| %>
    <p>
      <%= post.title %> | <%= link_to "View Post", post %>
    </p>
  <% end %>
</div>
