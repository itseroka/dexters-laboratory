<div id="<%= dom_id category %>">
  <p>
    <strong>Name:</strong>
    <%= category.name %>
  </p>

  <% category.posts.each do |post| %>
    <div class="content">
    <li><%= link_to post.title, post %></li>
  </div>
  <% end %>
</div>